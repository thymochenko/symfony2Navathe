{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Projeto</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Projnome</th>
                <td>{{ entity.projnome }}</td>
            </tr>
            <tr>
                <th>Projlocal</th>
                <td>{{ entity.projlocal }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('projeto') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('projeto_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
