{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Projeto list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Projnome</th>
                <th>Projlocal</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('projeto_show', { 'id': entity.id }) }}">{{ entity.projnome }}</a></td>
                <td>{{ entity.projlocal }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('projeto_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('projeto_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('projeto_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
