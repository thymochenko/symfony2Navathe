{% extends '::base.html.twig' %}

{% block body -%}
    <h1>LocalizacaoDep list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Dlocal</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('localizacaodep_show', { 'id': entity.id }) }}">{{ entity.dlocal }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('localizacaodep_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('localizacaodep_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('localizacaodep_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
