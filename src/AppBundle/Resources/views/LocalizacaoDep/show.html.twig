{% extends '::base.html.twig' %}

{% block body -%}
    <h1>LocalizacaoDep</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Dlocal</th>
                <td>{{ entity.dlocal }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('localizacaodep') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('localizacaodep_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
